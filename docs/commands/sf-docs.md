# sf docs

MkDocs を使用してドキュメントサイトを提供・ビルドします。

## 構文

```bash
sf docs <subcommand> [options]
```

### サブコマンド

| サブコマンド | 説明 |
|-------------|------|
| `serve` | 開発用サーバーを起動 |
| `build` | 静的サイトをビルド |

## 使用例

```bash
sf docs serve
sf docs serve --lan
sf docs build
```

> 詳細なドキュメントは今後追加予定です。
