\documentclass[border=5pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, calc}

\begin{document}

\definecolor{accentblue}{RGB}{0, 120, 200}
\definecolor{accentred}{RGB}{220, 60, 20}
\definecolor{accentgrn}{RGB}{0, 150, 60}
\definecolor{accentylw}{RGB}{200, 160, 0}
\definecolor{labelgray}{RGB}{60, 60, 60}
\begin{tikzpicture}[
    lbl/.style={font=\small\bfseries, text=labelgray, align=center,
                fill=white, fill opacity=0.85, text opacity=1,
                rounded corners=2pt, inner sep=3pt},
    arr/.style={-{Stealth[length=2.5mm]}, line width=1pt, accentblue},
    stickbox/.style={font=\small\bfseries, align=center,
                     draw=accentblue, fill=accentblue!10,
                     rounded corners=3pt, inner sep=4pt},
    btnbox/.style={font=\small\bfseries, align=center,
                   draw=accentylw!80!black, fill=accentylw!15,
                   rounded corners=3pt, inner sep=3pt},
  ]

  % Load controller photo
  % Path is relative to the main .tex that \input's this file
  \node[anchor=south west, inner sep=0] (img) at (0,0)
    {\includegraphics[width=10cm]{../images/controller.jpg}};

  % Set up relative coordinate system (0,0)-(1,1) over image
  \begin{scope}[
    x={(img.south east)},
    y={(img.north west)},
  ]

    % --- Left stick (MODE 3: Pitch / Roll) ---
    \node[stickbox, anchor=east] (lstick) at (-0.05, 0.30)
      {Pitch $\updownarrow$\\Roll $\leftrightarrow$};
    \draw[arr] (lstick.east) -- (0.18, 0.30);
    % Left stick push = Flip (planned)
    \node[lbl, anchor=north east, font=\scriptsize\bfseries] (lpress) at (-0.05, 0.16)
      {Push: Flip\\{\tiny (予定)}};
    \draw[arr, densely dashed] (lpress.east) -- (0.18, 0.22);

    % --- Right stick (MODE 3: Throttle / Yaw) ---
    \node[stickbox, anchor=west] (rstick) at (1.05, 0.28)
      {Throttle $\updownarrow$\\Yaw $\leftrightarrow$};
    \draw[arr] (rstick.west) -- (0.82, 0.28);
    % Right stick push = ARM
    \node[lbl, anchor=north west, font=\scriptsize\bfseries,
          draw=accentred, text=accentred] (rpress) at (1.05, 0.14)
      {Push: ARM};
    \draw[arr, accentred] (rpress.west) -- (0.82, 0.20);

    % --- Power switch ---
    \node[lbl, anchor=south] (pwr) at (0.53, 1.04) {Power SW};
    \draw[arr] (pwr.south) -- (0.52, 0.90);

    % --- Yellow button LEFT (Alt Hold / Manual) ---
    \node[btnbox, anchor=south east] (yleft) at (0.18, 1.04)
      {Alt Hold /\\Manual\\{\tiny (予定)}};
    \draw[arr, accentylw!80!black] (yleft.south) -- (0.16, 0.96);

    % --- Yellow button RIGHT (STABILIZE / ACRO) ---
    \node[btnbox, anchor=south west] (yright) at (0.82, 1.04)
      {STABILIZE /\\ACRO};
    \draw[arr, accentylw!80!black] (yright.south) -- (0.84, 0.96);

    % --- M5 Button = LCD panel ---
    \node[lbl, anchor=north, draw=accentgrn, text=accentgrn] (lcd) at (0.50, -0.06)
      {M5 Button (LCD)\\{\footnotesize タップでペアリング等}};
    \draw[arr, accentgrn] (lcd.north) -- (0.50, 0.18);

    % --- MODE 3 badge ---
    \node[font=\footnotesize\bfseries, text=white,
          fill=accentred, rounded corners=2pt, inner sep=3pt,
          anchor=north east] at (1.05, 1.04) {MODE 3};

  \end{scope}
\end{tikzpicture}
\end{document}
