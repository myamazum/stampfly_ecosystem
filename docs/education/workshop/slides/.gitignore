# LaTeX build intermediates
*.aux
*.nav
*.out
*.snm
*.toc
*.vrb

# Generated intermediate files (rebuild with `make all`)
tikz/*.pdf
pptx/output/*.pptx

# Beamer PDFs are tracked (committed for distribution)
# beamer/*.pdf
