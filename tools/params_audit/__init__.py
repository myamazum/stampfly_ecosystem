"""
params_audit — physical parameter consistency audit for StampFly.
params_audit — StampFly 物理パラメータ整合検査。

See README.md in this directory for what this checks and how to extend it.
Entry point: check_params.main() (also exposed as `sf params check`).
本ディレクトリの README.md に検査内容・拡張方法を記載。
エントリポイント: check_params.main()（`sf params check` からも呼ばれる）。
"""
